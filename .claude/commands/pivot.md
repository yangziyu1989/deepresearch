# Force Pivot

**Usage:** `/pivot <workspace_path>`

Force the pipeline to pivot to a new research idea at the next decision point. Sets a pivot flag that the state machine checks during `idea_validation_decision` or `experiment_decision`.
